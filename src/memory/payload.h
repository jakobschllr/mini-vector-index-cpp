


/*
- The graph nodes (stored in vectors.bin) only safe the vector-data and a unique id
- a key-value-index maps this node-ID to a unique offset-adress within a data-file which contains the chunks and other metadata
*/